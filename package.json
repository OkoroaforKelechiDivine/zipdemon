{  
  "name": "zipdemon",  
  "displayName": "zipDemon",  
  "description": "A VS Code extension that automatically generates documentation for newly created files, including class and method summaries, to keep your projects well-documented with ease.",  
  "version": "0.0.1",  
  "engines": {  
    "vscode": "^1.54.0"  
  },  
  "categories": [  
    "Other"  
  ],  
  "activationEvents": [  
    "onCommand:zipdemon.helloWorld"  
  ],  
  "main": "./extension.js",  
  "contributes": {  
    "commands": [  
      {  
        "command": "zipdemon.helloWorld",  
        "title": "Hello World"  
      },  
      {  
        "command": "zipdemon.generateDocumentation",  
        "title": "Generate Documentation"  
      }  
    ]  
  },  
  "scripts": {  
    "lint": "eslint .",  
    "pretest": "npm run lint",  
    "test": "vscode-test"  
  },  
  "devDependencies": {  
    "@types/vscode": "^1.54.0",  
    "@types/mocha": "^10.0.10",  
    "@types/node": "20.x",  
    "eslint": "^9.16.0",  
    "@vscode/test-cli": "^0.0.10",  
    "@vscode/test-electron": "^2.4.1"  
  }  
}